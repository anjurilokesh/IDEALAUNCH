function requireAuth(){ if(!sessionStorage.getItem('idealUser')) window.location.replace('index.html'); }
function logout(){ sessionStorage.removeItem('idealUser'); window.location.replace('index.html'); }
if(document.body.classList.contains('login-page')) { if(sessionStorage.getItem('idealUser')) window.location.replace('dashboard.html'); document.getElementById('loginForm').addEventListener('submit', e=>{e.preventDefault(); const name=document.getElementById('username').value.trim(); const password=document.getElementById('password').value; const error=document.getElementById('formError'); if(name.length<2) error.textContent='Please enter a name with at least 2 characters.'; else if(password.length<6) error.textContent='Your password needs at least 6 characters.'; else {sessionStorage.setItem('idealUser', name); if(!sessionStorage.getItem('savedIdeas')) sessionStorage.setItem('savedIdeas','[]'); window.location.href='dashboard.html';}});}
