const name=sessionStorage.getItem('idealUser');
document.getElementById('pageContent').innerHTML=`<section class="page-intro"><p class="eyebrow">YOUR LAUNCH ID</p><h1>Builder <em>profile.</em></h1><p>Manage how you show up inside the IDEALAUNCH community.</p></section><section class="profile-grid"><article class="profile-card"><div class="profile-avatar">${initials}</div><div><h2>${name}</h2><p>Idea explorer · Member since today</p></div><div class="profile-stat"><strong>${getSaved().length}</strong><span>saved signals</span></div></article><form class="profile-form" id="profileForm"><p class="eyebrow">PROFILE SETTINGS</p><h2>Make it yours.</h2><label>DISPLAY NAME<input id="profileName" value="${name}" maxlength="30"></label><label>FOCUS AREA<select><option>Exploring all sectors</option>${categories.slice(1).map(c=>`<option>${c}</option>`).join('')}</select></label><p class="success-msg" id="profileMessage"></p><button class="primary-btn" type="submit">Save changes <span>→</span></button></form></section>`;
document.getElementById('profileForm').onsubmit=e=>{e.preventDefault();const updated=document.getElementById('profileName').value.trim();if(updated.length<2)return;sessionStorage.setItem('idealUser',updated);document.getElementById('profileMessage').textContent='Profile updated successfully.';setTimeout(()=>location.reload(),700);};
