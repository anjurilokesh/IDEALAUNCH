const ideaId=Number(new URLSearchParams(location.search).get('id')); const idea=ideas.find(item=>item.id===ideaId)||ideas[0];
document.getElementById('pageContent').innerHTML=`<a class="back-link" href="explore.html">← Back to all signals</a><section class="detail-hero"><div class="detail-title"><div class="idea-icon large ${idea.color}">${idea.icon}</div><div><p class="eyebrow">${idea.category.toUpperCase()} · ${idea.tag.toUpperCase()}</p><h1>${idea.name}</h1><p>${idea.desc}</p></div></div><div class="detail-actions"><button class="save-big ${isSaved(idea.id)?'saved':''}" data-save="${idea.id}">${isSaved(idea.id)?'♥ Saved to watchlist':'♡ Save signal'}</button><span class="score"><b>${idea.score}</b><small>SIGNAL SCORE</small></span></div></section><section class="detail-grid"><article class="detail-main"><div><p class="eyebrow">THE OPPORTUNITY</p><h2>Why now?</h2><p>${idea.problem}</p></div><div><p class="eyebrow">THE PROPOSITION</p><h2>What it unlocks.</h2><p>${idea.solution}</p></div><div><p class="eyebrow">BUILDING BLOCKS</p><div class="tag-row">${idea.tags.map(t=>`<span>${t}</span>`).join('')}</div></div></article><aside class="signal-panel"><p class="eyebrow">SIGNAL SNAPSHOT</p><div class="metric-list">${idea.metrics.map((m,i)=>`<div><span>0${i+1}</span><strong>${m}</strong></div>`).join('')}</div><div class="market-note"><span>MARKET POTENTIAL</span><strong>${idea.market}</strong><small>${idea.stage} stage</small></div></aside></section>`;
bindSaveButtons();document.querySelector('[data-save]').onclick=()=>{const saved=toggleSaved(idea.id);const b=document.querySelector('[data-save]');b.classList.toggle('saved',saved);b.textContent=saved?'♥ Saved to watchlist':'♡ Save signal';};
