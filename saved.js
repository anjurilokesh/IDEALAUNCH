function renderSaved(){const saved=ideas.filter(idea=>getSaved().includes(idea.id)); document.getElementById('savedResults').innerHTML=saved.length?saved.map(ideaCard).join(''):`<div class="empty-state saved-empty"><span>♡</span><h3>Your watchlist is waiting.</h3><p>Save interesting signals while exploring and they’ll live here.</p><a href="explore.html" class="primary-btn inline-btn">Explore ideas <span>→</span></a></div>`; bindSaveButtons();}
document.getElementById('pageContent').innerHTML=`<section class="page-intro saved-intro"><p class="eyebrow">YOUR PERSONAL INDEX</p><h1>Signals you <em>saved.</em></h1><p>Keep the ideas with promise close, and come back when the timing is right.</p></section><div class="result-meta"><span><strong id="savedCount"></strong> signals on your watchlist</span><span>YOUR CURATED COLLECTION</span></div><section class="idea-grid" id="savedResults"></section>`; document.getElementById('savedCount').textContent=getSaved().length;renderSaved();
